# Database Configuration
POSTGRES_PASSWORD=calcom123

# Redis Configuration
REDIS_PASSWORD=redis123

# Application URL (Change this to your domain in production)
WEBAPP_URL=http://localhost:3000

# NextAuth Configuration (REQUIRED)
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-nextauth-key-change-this-in-production

# Encryption Key (REQUIRED - Must be 32 characters for AES256)
# Generate a key: openssl rand -hex 16
ENCRYPTION_KEY=your-32-character-encryption-key

# Email Configuration (Required for user registration and notifications)
EMAIL_FROM=noreply@yourdomain.com
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=465
EMAIL_SERVER_USER=your-gmail@gmail.com
EMAIL_SERVER_PASSWORD=your-app-password

# Optional: SendGrid Configuration
# SENDGRID_API_KEY=your-sendgrid-api-key
# SENDGRID_EMAIL=your-sendgrid-sender@yourdomain.com

# Application Settings
TEAM_IMPERSONATION=false
SCHEDULE_INTERVAL=15

# Cron Configuration
CRON_API_KEY=your-cron-api-key-for-scheduled-tasks
CRON_ENABLE_APP_SYNC=false

# Optional: Analytics and Telemetry
# TELEMETRY_KEY=your-telemetry-key

# License Consent
LICENSE_CONSENT=agree

# ===================================
# PRODUCTION DEPLOYMENT NOTES:
# ===================================
# 1. Change WEBAPP_URL to your actual domain (e.g., https://cal.yourdomain.com)
# 2. Generate secure secrets for NEXTAUTH_SECRET and ENCRYPTION_KEY
# 3. Configure proper email settings
# 4. Set strong passwords for POSTGRES_PASSWORD and REDIS_PASSWORD
# 5. Consider using Docker secrets for sensitive data in production
# 6. Enable SSL/TLS by configuring nginx with proper certificates
# ===================================
